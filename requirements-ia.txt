# Dependencias para enriquecimiento IA
# Optimizadas para CPU y bajo consumo de memoria

# PyTorch CPU-only (más ligero que versión completa)
torch==2.1.0+cpu; platform_system == "Linux"
torch==2.1.0; platform_system != "Linux"
--index-url https://download.pytorch.org/whl/cpu

# Transformers de Hugging Face
transformers==4.35.0

# Utilidades
numpy==1.24.3
pandas==2.0.3
requests==2.31.0

# Firebase (opcional, solo si se quiere subir resultado)
pyrebase4==4.7.1

# Procesamiento de texto
regex==2023.10.3
