# Dependencias para enriquecimiento IA con Groq
# Versiones fijas para evitar conflictos de dependencias

# Cliente Groq API
groq==0.9.0

# HTTP requests
requests==2.31.0
urllib3==2.0.7

# Firebase (para subir/descargar de storage)
pyrebase4==4.7.1

# Dependencias de pyrebase4 que pueden causar conflictos
pycryptodome==3.20.0
python-jwt==4.1.0
